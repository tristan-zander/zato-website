
// TODO change to an actual image instead of a URL
export default interface Framedata {
    imageUrl: string;
    description: string;
    moveTitle: string;
    startupFrames: Number;
    activeFrames: Number;
    recoveryFrames: Number;
    onBlockFrames: Number;
}